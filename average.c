#include<stdio.h>
int main(){
    // int physicsmarks=73;
    // int mathmarks=65;
    // int chemistrymarks=40;
    // int englishmarks=60;
    // printf("average of marks is : %d",(physicsmarks+ mathmarks+chemistrymarks+englishmarks)/4);
    int a;
    int b;
    printf("enter 1st no. ");
    scanf("%d",&a);
printf("enter 2nd no. ");
scanf("%d",&b);
    printf("(a+b)^2 is :%d",(a*a+b*b+2*a*b));
// (a*a+b*b+2*a*b),
    return 0;
}